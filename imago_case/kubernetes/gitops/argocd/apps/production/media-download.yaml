apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: media-download
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: imago
  source:
    repoURL: https://github.com/sibinnediyoram/demo_repository
    targetRevision: main
    path: imago_case/kubernetes/overlays/production/media-download
  destination:
    server: https://kubernetes.default.svc
    namespace: imago-download
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    - group: apps
      kind: Deployment
      name: media-download
      jsonPointers:
        - /spec/replicas
