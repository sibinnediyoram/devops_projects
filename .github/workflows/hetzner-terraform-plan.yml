name: Hetzner Terraform Plan

on:
  pull_request:
    paths:
      - "hetzner_k8s_infrastructure/terraform/**"
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: Environment to plan
        type: choice
        options: [staging, production]
        default: staging

permissions:
  contents: read
  pull-requests: write

jobs:
  detect-changes:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    outputs:
      environments: ${{ steps.detect.outputs.environments }}
    steps:
      - uses: actions/checkout@v4
      - id: detect
        name: Detect changed environments
        run: |
          changed=$(git diff --name-only origin/main...HEAD -- hetzner_k8s_infrastructure/terraform/environments/ | \
            grep -oP 'hetzner_k8s_infrastructure/terraform/environments/\K[^/]+' | sort -u | jq -R -s -c 'split("\n")[:-1]')
          echo "environments=$changed" >> $GITHUB_OUTPUT

  plan:
    needs: detect-changes
    if: |
      always() &&
      (github.event_name == 'workflow_dispatch' ||
       (needs.detect-changes.result == 'success' && needs.detect-changes.outputs.environments != '[]'))
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: ${{ github.event_name == 'workflow_dispatch' && fromJson(format('["{0}"]', inputs.environment)) || fromJson(needs.detect-changes.outputs.environments) }}
    environment: ${{ matrix.environment }}
    defaults:
      run:
        working-directory: hetzner_k8s_infrastructure/terraform/environments/${{ matrix.environment }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "~1.6"

      - name: Terraform Init
        run: terraform init -input=false
        env:
          HCLOUD_TOKEN: ${{ secrets.HCLOUD_TOKEN }}

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan -input=false -no-color -out=tfplan 2>&1 | tee plan-output.txt
          echo "exitcode=$?" >> $GITHUB_OUTPUT
        env:
          HCLOUD_TOKEN: ${{ secrets.HCLOUD_TOKEN }}
          TF_VAR_cluster_token: ${{ secrets.CLUSTER_TOKEN }}
          TF_VAR_ssh_key_name: ${{ secrets.SSH_KEY_NAME }}
          TF_VAR_bastion_ips: ${{ secrets.BASTION_IPS }}

      - name: Post Plan to PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const output = fs.readFileSync('hetzner_k8s_infrastructure/terraform/environments/${{ matrix.environment }}/plan-output.txt', 'utf8');
            const truncated = output.length > 65000 ? output.slice(-65000) + '\n...(truncated)' : output;
            const body = `### Terraform Plan — \`${{ matrix.environment }}\`
            \`\`\`hcl
            ${truncated}
            \`\`\``;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
