apiVersion: v1
kind: Service
metadata:
  name: web-app
  namespace: hetzner_demo-web
  labels:
    app: web-app
spec:
  selector:
    app: web-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
